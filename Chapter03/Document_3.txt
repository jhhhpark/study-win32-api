윈도우즈는 세 가지 동적 연결 라이브러리(DLL)로 구성되어 있다.
메모리 관리, 프로그램 실행 : KERNEL
유저 인터페이스와 윈도우 관리 : USER
화면상의 처리 및 그래픽 : GDI(Graphic Device Interface)

여기서는 특히 GDI 모듈에 대해 관심을 갖자.
화면상에 그려지는 모든 것들은 GDI를 통해야 한다.

DC(Device Context)란 출력에 필요한 모든 정보를 가지는 데이터 구조체이며 
GDI 모듈에 의해 관리된다.(DC에는 선의 색상, 굵기, 채움, 무늬, 그리기 모드 등등)

